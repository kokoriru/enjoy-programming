<%= form_with(model: p, local: true) do |form| %>
  <% if p.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(p.errors.count, "error") %> prohibited this p from being saved:</h2>

      <ul>
      <% p.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_area :code %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
