<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @p.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= simple_format(h(@p.description)) %>
</p>

<p>
  <strong>Code:</strong>
  <%= simple_format(h(@p.code)) %>
</p>

<%= link_to 'Edit', edit_p_path(@p) %> |
<%= link_to 'Back', ps_path %>
